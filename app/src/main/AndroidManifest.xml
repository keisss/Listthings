<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keiss.listthings">
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".util.ContextGetter"

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AddThingsActivity"
            android:label="@string/title_activity_add_things_">
        </activity>
        <activity android:name=".ui.UpdateThingsActivity"
            android:label="Update_Things_Activity">
        </activity>
    <activity android:name=".ui.CalendarModeActivity"
        android:label="Calendar_Mode_Activity">
    </activity>
        <activity android:name=".ui.DrawYourPic"
            android:label="Draw_your_Pic">

        </activity>
        <activity android:name=".ui.AboutActivity"
            android:label="About_Activity"/>
    </application>

</manifest>